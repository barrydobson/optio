apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: optio-gateway
  namespace: optio
spec:
  gatewayClassName: envoy
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: optio.test
