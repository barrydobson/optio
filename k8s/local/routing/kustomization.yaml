apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply after infra is ready:
#
#   kubectl apply -k k8s/local/routing
resources:
  - gateway-class.yaml
  - gateway.yaml
  - httproute.yaml
